# OpenCode v2 manages its server password in the service user's persistent
# configuration. Provider credentials are intentionally configured separately
# and are not baked into the Proxmox template.
{ ... }: { }
